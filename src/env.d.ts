// Compile-time constants injected by Vite `define` (see vite.config.ts).
// `__APP_VERSION__` is the `version` field from package.json, the single source
// of truth for the version shown in the footer.
declare const __APP_VERSION__: string;
